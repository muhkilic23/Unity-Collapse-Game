using UnityEngine;

public static class SettingsHolder {
    public static int Rows = 8;              //can be changed via menu
    public static int Cols = 8;              //can be changed via menu
    public static int colors = 4;            //can be changed via menu
    public static int firstCondition = 3;    // must be changed in here
    public static int secondCondition = 5;   // must be changed in here
    public static int thirdCondition = 7;    // must be changed in here
}
